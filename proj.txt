to modules:
add znPaths {0: path...}
add parPaths {w: path...}

add runPaths to run paths

ui which executes command(doesnt add script)
keyboard stuff uses new command

command to set/change path

for ui, a path function has some meta data

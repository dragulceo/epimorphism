DISTANCE FIELD:

- make distance_field component
  + basic renderer
  - textures
    - switching textures
      - just re-use tex-color_scroll or something like that
      - input is wdt.zw, output is seed
  - multiple shapes
    - blend between shapes, w script
  - lighting?
    - move light, change colors?
  - normals?
    - animated?
  - animation: spinning, etc    
+ make distance_field module
- integrate module into current program
  - one of the subcomponents of seed_multi
  + seed2?
  - script on seed_multi_anim which switches between seed1s?


- fog?
- anti-aliasing
